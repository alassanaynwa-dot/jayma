# Dépendances production — épinglées strictement avec ==.
# Pour mettre à jour : modifier la version, tester en local, deploy.
# Renforcer les versions via PR (Dependabot ou manuel).
# Dernière revue : 2026-04-29.

# Django 5.2 LTS (support jusqu'en avril 2028)
Django==5.2.13

# Admin moderne
django-unfold==0.90.0

# Base de données
psycopg[binary]==3.3.3

# Cache / queue
redis==6.4.0
django-redis==6.0.0
celery[redis]==5.6.3
django-celery-beat==2.9.0
django-timezone-field==7.2.1  # dep transitive de django-celery-beat (épinglée pour reproductibilité)

# Formulaires & UI
django-htmx==1.27.0
Pillow==12.2.0

# Utilitaires
python-dotenv==1.2.2
django-environ==0.13.0
django-extensions==4.1
requests==2.33.1

# Stockage fichiers (R2 / S3)
boto3==1.42.91
botocore==1.42.91
django-storages==1.14.6

# Static files en prod
whitenoise==6.12.0

# SMS
africastalking==2.0.2

# Serveur prod
gunicorn==25.3.0

# Monitoring erreurs prod
sentry-sdk[django]==2.20.0

# Rate limiting (login, OTP, signup, stock alerts)
django-ratelimit==4.1.0
